{
  "name": "interviewbuddy",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "setup": "npm install --prefix client && npm install --prefix server",
    "build": "npm build --prefix client",
    "start": "npm run dev --prefix server && npm run dev --prefix client",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Varenyam Singh",
  "license": "ISC",
  "description": ""
}
